a = [1, 3, 5, 7, 9]
# similar to forever loop:
while True:
    # access by value
    #for element in a:
    #    basic.show_number(element)
    # access by index
    for index in range(len(a)):
        basic.show_number(index)
        basic.show_number(a[i]) # a[i] element in the list a at address i
